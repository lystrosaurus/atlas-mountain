generator:
  datasource:
    url: jdbc:mysql://localhost:3306/atlas_mountain?remarks=true&useInformationSchema=true
    username: root
    password: ""  # 请替换为本地数据库密码

  output-dir: /tmp/generated

  package:
    parent: io.github.lystrosaurus.atlasmountain
    module: order

  tables:
    - sys_order
    - sys_order_item
